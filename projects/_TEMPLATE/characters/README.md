# characters/

Approved character reference stills go here, named `REF-<name>-still.png`.

Generate them with `mcp__kagelot__generate_image` and keep the approved one. A character reference is a **full still** of the character — KAGELOT uses it to keep that character consistent across clips.
